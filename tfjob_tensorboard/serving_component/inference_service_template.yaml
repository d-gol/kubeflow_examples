apiVersion: "serving.kubeflow.org/v1alpha2"
kind: "InferenceService"
metadata:
  name: MODEL_NAME_TO_REPLACE
  namespace: NAMESPACE_TO_REPLACE
spec:
  default:
    predictor:
      tensorflow:
        runtimeVersion: RUNTIME_VERSION_TO_REPLACE
        storageUri: STORAGE_TO_REPLACE
